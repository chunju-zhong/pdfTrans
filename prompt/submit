### 提交修改

1. 确认当前分支是最新的，使用 `git pull` 命令。
2. 查看当前项目状态，使用 `git status` 命令。
3. 暂存所有修改的文件，使用 `git add .` 命令。
4. 提交修改到本地仓库，使用 `git commit -m "描述性提交信息"` 命令。
5. 推送修改到远程仓库，使用 `git push` 命令。

### 注意事项

- 确保在提交前备份重要数据，以防意外情况发生。
- 编写有意义的提交信息，以便其他开发人员理解所做的修改。

### 示例

假设你在 `feature/translation` 分支上工作，你可以按照以下步骤提交修改：

1. 确认当前分支是 `feature/translation`。
2. 查看当前项目状态，发现有两个文件被修改：`app.py` 和 `translation.py`。
3. 暂存所有修改的文件，使用 `git add .` 命令。
4. 提交修改到本地仓库，使用 `git commit -m "描述性提交信息"` 命令。
5. 推送修改到远程仓库，使用 `git push` 命令。
