# Tasks

- [x] Task 1: 修改 BLOCK_TYPE_MAP 中标题类型的 is_body_text 映射
  - [x] 将 `title` 的映射从 `(False, 1)` 改为 `(True, 1)`
  - [x] 将 `sub_title` 的映射从 `(False, 1)` 改为 `(True, 1)`
  - [x] 将 `section_title` 的映射从 `(False, 1)` 改为 `(True, 1)`
  - [x] 保持 `header`、`footer`、`footnote`、`page_number` 的 `is_body_text=False` 不变

# Task Dependencies
- 无
