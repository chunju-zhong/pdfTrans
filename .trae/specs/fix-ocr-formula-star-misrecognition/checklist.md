## OCR 误识别修复

- [ ] `0.6^{*}` 被修正为 `0.6 \cdot`
- [ ] `0.72^{*}` 被修正为 `0.72 \cdot`
- [ ] `1.33^{*}` 被修正为 `1.33 \cdot`
- [ ] `x^{*}`（变量）保持不变
- [ ] `(...)^{*}`（括号）保持不变
- [ ] 正常 `\cdot` 不受影响

## OMML 验证

- [ ] 修正后 latex2mathml 输出无 `<msup>` 结构包裹数字
- [ ] 数字在 OMML 中作为普通 `<m:r>` 而非 `<m:sSup>` 底数渲染

## 整体验证

- [ ] 语法检查通过
- [ ] 现有测试通过