# Tasks

- [x] Task 1: 修改 `_parse_html_table` 方法，联合计算列宽和行高
  - [x] SubTask 1.1: 实现按内容权重分配列宽的逻辑（CJK字符宽度按字体大小×1.0估算，ASCII按字体大小×0.6估算，取每列最大显示宽度作为权重，最小列宽10%，最大列宽50%）
  - [x] SubTask 1.2: 实现根据列宽联合计算行高的逻辑（估算每行每个单元格在给定列宽下的换行行数，取每行最大行数，行高=行数×单行高度）
  - [x] SubTask 1.3: 行高钳位到原始bbox范围内（超出则按比例缩小，不足则按比例放大填满）
  - [x] SubTask 1.4: 根据新的列宽和行高重新计算所有单元格的bbox
  - [x] SubTask 1.5: 添加日志记录新的列宽和行高分配结果
- [x] Task 2: 在 `_draw_translated_table` 方法中增加文本溢出截断机制
  - [x] SubTask 2.1: 在5次缩小字体失败后，增加文本截断逻辑：对CJK文本逐字符截断，对英文文本按空格分词截断
  - [x] SubTask 2.2: 截断时每次减少1个字符/词，重新调用 `insert_textbox` 直到返回非负值，截断后末尾加"…"
  - [x] SubTask 2.3: 如果截断到只剩1个字符+省略号仍溢出，使用最小字体强制写入
- [x] Task 3: 增加表格单元格渲染日志
  - [x] SubTask 3.1: 在单元格文本溢出时记录单元格坐标、文本前50字、单元格尺寸、最终处理方式

# Task Dependencies
- Task 2 和 Task 3 依赖 Task 1（先在识别阶段优化尺寸分配，绘制阶段截断作为兜底）
- Task 3 依赖 Task 2（日志需要记录截断结果）
