- [x] `_compute_table_grid` 累积行高等于表格 bbox 总高度
- [x] `_compute_table_grid` 累积列宽等于表格 bbox 总宽度
- [x] 单元格循环不再绘制边框
- [x] 外框使用 `draw_rect` 统一绘制
- [x] 内部水平线使用 `draw_line` 绘制，从表格左到右边界
- [x] 内部垂直线使用 `draw_line` 绘制，从表格上到下边界
- [x] 语法检查和单元测试通过
- [ ] PDF 输出中外框、横线、竖线完整（需要用户运行验证）