# Checklist

## 布局格式化提示词公式保护

- [x] `_load_layout_prompt()` 的系统提示词中包含公式保护规则
- [x] 规则明确要求不修改、删除或转义 `$...$` 和 `$$...$$` 标记
- [x] 规则明确要求公式内容原样保留，不添加空格、换行或解释
- [x] 规则包含正确/错误示例

## 行内/独立行公式区分

- [x] `_process_page_elements()` 中公式包裹逻辑根据 bbox 宽度占比区分行内/独立行
- [x] 独立行公式（宽度占比 ≥ 60%）使用 `$$...$$` 包裹
- [x] 行内公式（宽度占比 < 60%）使用 `$...$` 包裹
- [x] `_insert_element_in_merged_block()` 中同样区分行内/独立行公式

## 端到端验证

- [ ] 包含 `$E=mc^2$` 的输入文本经 LLM 格式化后 `$` 标记被完整保留
- [ ] 包含 `$$\frac{a}{b}$$` 的输入文本经 LLM 格式化后 `$$` 标记被完整保留
- [ ] 公式内容不被加粗、斜体或其他格式化
