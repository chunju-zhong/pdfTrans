- [x] `_preprocess_text` 保留列表换行：按 `\n` 分割、每行内压缩空白、过滤空行、再用 `\n` 重新连接

- [x] `_postprocess_text` 保留列表换行：按 `\n` 分割、每行内压缩空白、过滤空行、再用 `\n` 重新连接

- [x] 翻译 prompt 新增规则 14：保持原文列表格式，保留换行和项目符号

- [x] `pytest tests/ -x -q` 全部通过（362 passed, 1 skipped）
