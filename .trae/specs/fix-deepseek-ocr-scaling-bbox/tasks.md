# Tasks

- [x] Task 1: 调查图像在 PDF 中的实际绘制信息
  - [x] 1.1: 使用 `page.get_images(full=True)` 获取第 8 页的嵌入图像列表
  - [x] 1.2: 使用 `page.get_image_rects(xref)` 获取图像在 PDF 页面中的边界框
  - [x] 1.3: 对比图像边界框与页面尺寸，确认是否有边距/缩放/偏移
  - [x] 1.4: 分析多个页面的图像绘制模式（是否一致）
  - [x] 1.5: **关键发现**：图像边界框 = (0, 0, 1191.0, 231.8)，完全覆盖页面

- [x] Task 2: 验证坐标转换假设
  - [x] 2.1: 确认当前转换公式 `pdf = normalized / 999 * page_size` 的假设
  - [x] 2.2: 如果图像边界框 ≠ 页面尺寸，计算正确的转换公式
  - [x] 2.3: 使用实际图像边界框重新计算示例坐标，验证是否解决偏移
  - [x] 2.4: **关键发现**：白边理论错误！`page.get_pixmap()` 渲染的是整个 PDF 页面，白边是页面本身的一部分

- [x] Task 3: 删除错误的白边检测代码
  - [x] 3.1: 删除 `llm_response_parser.py` 中的 `_detect_image_margins` 方法
  - [x] 3.2: 删除 `llm_extractor.py` 中的白边检测调用和相关代码
  - [x] 3.3: 删除 `_pixel_to_pdf_coords` 中的 `content_region` 分支
  - [x] 3.4: 更新 spec.md 修正错误的根因分析

- [ ] Task 4: 重新调查文本框偏移的真正原因
  - [ ] 4.1: 收集第 8 页的实际坐标数据（DeepSeek-OCR 返回的归一化坐标、转换后的 PDF 坐标）
  - [ ] 4.2: 对比实际文本位置和转换后的坐标，计算偏移量
  - [ ] 4.3: 分析偏移模式（是否所有文本块都有相同的偏移？）
  - [ ] 4.4: 确定偏移的真正原因（DeepSeek-OCR 返回的坐标本身有问题？还是其他原因？）

- [ ] Task 5: 根据调查结果实施修复
  - [ ] 5.1: 根据 Task 4 的调查结果，确定修复方案
  - [ ] 5.2: 实施修复代码
  - [ ] 5.3: 验证修复效果

# Task Dependencies

- Task 3 已完成（删除错误的白边检测代码）
- Task 4 是当前的调查任务
- Task 5 依赖于 Task 4 的调查结果
