## 1. 修改 translation_service.py 中的 process_merged_blocks 方法
- 更新第 290 行：`first_text_block = first_block['text_block']` → `first_text_block = first_block`
- 更新第 295 行：`'page_num': first_block['page_num']` → `'page_num': first_block.page_num`
- 更新第 305 行：`text_block = block_info['text_block']` → `text_block = block_info`
- 更新第 330 行：`page_num = original_block_info['page_num']` → `page_num = original_block_info.page_num`

## 2. 验证修改
- 运行测试确保所有功能正常工作
- 验证翻译流程是否正确使用 TextBlock 对象的属性访问