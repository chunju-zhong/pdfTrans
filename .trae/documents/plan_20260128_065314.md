## 测试用例优化计划

### 1. 更新 AipingTranslator 测试用例
- **文件**: `tests/test_aiping_translator.py`
- **修改内容**:
  - 在所有测试方法中，创建 `AipingTranslator` 实例时添加 `api_url` 和 `model` 参数
  - 使用测试值如 `"https://test-api.aiping.cn/v1"` 和 `"test-model"`
  - 保持其他测试逻辑不变

### 2. 更新 SiliconFlowTranslator 测试用例
- **文件**: `tests/test_silicon_flow_translator.py`
- **修改内容**:
  - 在所有测试方法中，创建 `SiliconFlowTranslator` 实例时添加 `api_url` 和 `model` 参数
  - 使用测试值如 `"https://test-api.siliconflow.cn/v1"` 和 `"test-model"`
  - 保持其他测试逻辑不变

### 3. 更新 Translator 基类测试用例
- **文件**: `tests/test_translator.py`
- **修改内容**:
  - 在 `test_translate_basic` 和 `test_translate_same_language` 方法中，调用 `translate` 时添加 `doc_type` 和 `glossary` 参数
  - 使用测试值如 `"测试文档"` 和 `""`
  - 在 `test_aiping_translate` 和 `test_silicon_flow_translate` 方法中，创建实例时添加所需参数

### 4. 验证修改
- 运行所有测试用例，确保全部通过
- 确保测试覆盖率保持在高水平
- 确保测试逻辑仍然有效

## 预期结果
- 所有测试用例通过，无失败
- 测试代码与当前实现保持一致
- 测试覆盖率不降低
- 测试逻辑仍然能够有效验证功能