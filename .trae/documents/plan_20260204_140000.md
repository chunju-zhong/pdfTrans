## 1. 创建 MergedBlock 类
- 在 `models/` 目录下创建新文件 `merged_block.py`
- 定义 `MergedBlock` 类，包含以下属性：
  - block_text: 合并后的文本
  - original_blocks: 原始TextBlock对象列表
  - max_width: 原始块最大宽度
  - max_height: 原始块最大高度
  - font: 字体名称（来自第一个原始块）
  - font_size: 字体大小（来自第一个原始块）
  - color: 颜色值（来自第一个原始块）
  - flags: 样式标记（来自第一个原始块）
  - bold: 是否粗体（来自第一个原始块）
  - italic: 是否斜体（来自第一个原始块）
  - page_num: 页码（来自第一个原始块）
- 实现 `__init__` 方法初始化这些属性

## 2. 修改 merge_semantic_blocks 函数
- 在 `utils/text_processing.py` 中导入 `MergedBlock` 类
- 修改函数，将字典创建改为 `MergedBlock` 对象创建
- 在创建 MergedBlock 对象时，从第一个原始块中提取样式信息和页码
- 更新函数文档字符串，将返回类型从字典列表改为 MergedBlock 对象列表

## 3. 更新 translation_service.py 中的使用
- 修改 `process_merged_blocks` 方法，将字典访问改为对象属性访问：
  - `merged_block['original_blocks']` → `merged_block.original_blocks`
  - `merged_block['block_text']` → `merged_block.block_text`
  - `merged_block['max_width']` → `merged_block.max_width`
  - `merged_block['max_height']` → `merged_block.max_height`
- 更新 merged_translations 的创建，直接使用 merged_block 的属性：
  - `'page_num': merged_block.page_num`
  - `'font': merged_block.font`
  - `'font_size': merged_block.font_size`
  - `'color': merged_block.color`
  - `'bold': merged_block.bold`
  - `'italic': merged_block.italic`
- 移除重复的样式信息提取代码（第 318-325 行）

## 4. 更新测试文件
- 修改 `test_pdf_page_translation.py` 中的模拟数据，使用 `MergedBlock` 对象
- 修改 `test_semantic_merge_extended.py` 中的字典访问，改为对象属性访问
- 修改 `test_semantic_merge.py` 中的字典访问，改为对象属性访问

## 5. 验证修改
- 运行测试确保所有功能正常工作
- 验证合并块对象的使用是否正确
- 检查样式信息是否从合并块对象中正确获取