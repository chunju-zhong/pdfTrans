## Analysis

The issue is that a specific table isn't being inserted before the text "表1：认证流程中不同参与方类别的非完整示例" in Markdown generation. The problem could be related to:

1. **Table bbox information** - The table might not have proper bbox coordinates
2. **Position calculation** - The code might not correctly identify where to insert the table
3. **Merged block identification** - The code might not find the right merged block containing the reference text
4. **Insertion logic** - The table might be inserted in the wrong position

## Plan

Add targeted logging to understand what's happening during table processing:

### 1. Add logging when processing tables
- Log table properties (page_num, table_idx, bbox)
- Log when a table is being processed

### 2. Add logging to position calculation
- Log the before_block and after_block found for each table
- Log the y-coordinate comparison between table and original blocks

### 3. Add logging to merged block identification
- Log which merged block is found for each table
- Log the position (before/after) determined for insertion

### 4. Add logging to insertion process
- Log when a table is inserted before/after a merged block
- Log the content of the merged block being inserted around
- Log when a table is added to the end

### 5. Add specific logging for the target text
- Log when the text "表1：认证流程中不同参与方类别的非完整示例" is found in a merged block
- Log the relationship between this text and any nearby tables

## Implementation

Add these logging statements to the existing code without modifying the core logic, focusing on the key points in the table processing workflow.