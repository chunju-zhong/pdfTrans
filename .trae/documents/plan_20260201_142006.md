## 重构PDF表格提取逻辑

### 任务分析
用户要求：
1. 从现有的 `extract_text` 方法中提取表格提取的部分
2. 创建一个新的 `extract_text` 函数来处理表格提取
3. 删除原来的 `extract_text` 函数不再使用

### 重构方案

#### 1. 分析现有代码结构
- **目标文件**：`modules/pdf_extractor.py`
- **现有方法**：
  - `extract_text`：提取PDF文本和表格，包含详细的表格处理逻辑
  - `extract_tables`：仅提取PDF中的表格内容，使用简单实现

#### 2. 提取表格提取逻辑
- **创建新函数**：重构后的 `extract_text` 函数
- **功能**：提取PDF文本和表格，保持原有的表格处理逻辑
- **实现**：
  - 保留原 `extract_text` 方法的核心功能
  - 优化表格提取逻辑
  - 确保使用定制化的表格解析参数

#### 3. 删除原函数
- **删除文件**：原有的 `extract_text` 方法
- **更新调用**：确保所有调用 `extract_text` 的地方使用新函数

#### 4. 优化表格提取
- **应用定制化参数**：使用之前验证过的表格解析参数
- **增强错误处理**：添加更详细的错误处理和日志记录
- **提高鲁棒性**：确保能够处理各种类型的表格结构

### 实施步骤

1. **分析现有代码**：仔细分析 `extract_text` 方法中的表格提取逻辑
2. **创建新函数**：实现重构后的 `extract_text` 函数
3. **测试新函数**：确保功能正常工作
4. **删除原函数**：移除原来的 `extract_text` 方法
5. **验证集成**：确保所有调用点都正确更新

### 预期效果

- **功能统一**：所有表格提取都使用相同的逻辑
- **代码清晰**：函数职责明确，结构合理
- **性能优化**：避免重复的表格提取操作
- **可维护性**：减少代码重复，提高可维护性

### 技术要点

- **表格解析参数**：使用之前验证过的定制化参数
- **错误处理**：添加详细的错误处理和日志记录
- **兼容性**：确保新函数与原有调用方式兼容
- **测试覆盖**：确保各种表格结构都能正确处理