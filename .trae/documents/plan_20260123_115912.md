# 优化PDF提取流程

## 1. 优化目标

按照用户要求优化PDF提取流程，实现：

1. 创建独立的TextBlock类作为文本块模型
2. 先提取完整块，再更新详细样式信息
3. 按垂直位置从上到下排序文本块

## 2. 当前实现分析

* **现有流程**：
  * 先提取`blocks`级别（完整文本块）
  * 再提取`dict`级别（详细样式信息）
  * 但两者是分离的，没有完全整合

* **现有问题**：
  * 文本块信息分散在多个字段中
  * 没有统一的文本块模型
  * 没有显式按垂直位置排序

## 3. 优化方案

### 3.1 创建TextBlock模型类

创建独立的TextBlock类，封装所有显示相关信息：

```python
class TextBlock:
    """文本块模型
    
    封装PDF文本块的所有属性，包括内容和样式信息
    """
    
    def __init__(self, block_no, text, bbox, block_type=0):
        """初始化文本块
        
        Args:
            block_no (int): 块序号
            text (str): 文本内容
            bbox (tuple): 块边界框 (x0, y0, x1, y1)
            block_type (int): 块类型
        """
        self.block_no = block_no
        self.block_text = text.strip()
        self.block_bbox = bbox
        self.block_type = block_type
        # 样式信息
        self.font = ""
        self.font_size = 0.0
        self.color = 0
        self.flags = 0
        self.bold = False
        self.italic = False
        self.underline = False
        self.strikethrough = False
    
    def update_style(self, font="", font_size=0.0, color=0, flags=0):
        """更新文本块样式信息
        
        Args:
            font (str): 字体名称
            font_size (float): 字体大小
            color (int): 颜色值
            flags (int): 样式标记
        """
        self.font = font
        self.font_size = font_size
        self.color = color
        self.flags = flags
        # 解析样式标记
        self.bold = bool(flags & 1)
        self.italic = bool(flags & 2)
        self.underline = bool(flags & 4)
        self.strikethrough = bool(flags & 8)
    
    def to_dict(self):
        """转换为字典格式
        
        Returns:
            dict: 包含所有属性的字典
        """
        return {
            'block_no': self.block_no,
            'block_text': self.block_text,
            'block_bbox': self.block_bbox,
            'block_type': self.block_type,
            'font': self.font,
            'font_size': self.font_size,
            'color': self.color,
            'flags': self.flags,
            'bold': self.bold,
            'italic': self.italic,
            'underline': self.underline,
            'strikethrough': self.strikethrough
        }
```

### 3.2 优化提取流程

1. **创建TextBlock模型文件**：在合适位置创建TextBlock类
2. **优化提取流程**：
   * 提取完整文本块
   * 使用TextBlock类创建对象
   * 提取详细样式信息
   * 更新TextBlock对象的样式
   * 按位置排序
3. **修改返回结构**：确保返回数据使用统一的TextBlock结构

### 3.3 代码修改点

1. **创建`models/text_block.py`**：定义TextBlock类
2. **修改`extract_text`方法**：
   * 使用TextBlock类创建文本块
   * 整合样式更新逻辑
   * 添加按位置排序
3. **修改`extract_text_blocks`方法**：确保一致性

## 4. 详细实现步骤

### 4.1 创建TextBlock模型

* 创建`models`目录（如果不存在）
* 创建`text_block.py`文件，实现TextBlock类

### 4.2 优化提取流程

```python
# 提取完整文本块
blocks = page.get_text("blocks", flags=1)

# 创建TextBlock对象列表
text_blocks = []
for block in blocks:
    x0, y0, x1, y1, text, block_no, block_type = block
    if block_type == 0 and text.strip():
        text_block = TextBlock(block_no, text, (x0, y0, x1, y1), block_type)
        text_blocks.append(text_block)

# 提取详细样式信息并更新
text_dict = page.get_text("dict", flags=1)
# ... 更新TextBlock对象样式 ...

# 按垂直位置排序
text_blocks.sort(key=lambda block: block.block_bbox[1])

# 转换为字典格式返回
return_blocks = [block.to_dict() for block in text_blocks]
```

### 4.3 样式信息合并

使用矩形匹配算法将dict模式的样式信息合并到TextBlock对象中，确保每个文本块获得正确的样式信息。

## 5. 预期效果

* 统一的TextBlock模型，便于后续使用
* 完整的文本块信息，包含内容和样式
* 按垂直位置从上到下排序的文本块
* 更好的代码结构和可维护性
* 便于后续扩展和功能增强

## 6. 测试验证

* 测试提取功能是否正常
* 验证TextBlock对象包含所有要求的属性
* 检查块是否按位置正确排序
* 测试翻译功能是否正常工作

