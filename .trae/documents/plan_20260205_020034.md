# 修复图像边界框异常问题

## 问题分析

图像边界框异常的根本原因是代码错误地使用了 `img[1:5]` 作为图像边界框，而不是使用 `page.get_image_rects(xref)` 方法获取图像的实际边界框。

**当前问题**：
- 图像边界框为 `(145, 1524, 968, 8)`，其中 y0=1524 > y1=8，格式异常
- 导致图像在 Word 文档中位置不正确
- 与文本块的相对顺序错误

## 解决方案

### 1. 修改 `_extract_images` 方法
修改 `/Users/chunju/work/pdfTrans/modules/pdf_extractor.py` 文件的 `_extract_images` 方法：

- **替换边界框获取方式**：
  - 移除直接使用 `img[1:5]` 作为边界框的代码
  - 使用 `page.get_image_rects(xref)` 方法获取图像的实际边界框
  - 确保边界框格式正确（y0 < y1）

- **添加错误处理**：
  - 处理 `get_image_rects` 可能返回空列表的情况
  - 添加边界框验证，确保格式正确

### 2. 实现步骤

#### 2.1 更新 `_extract_images` 方法
```python
def _extract_images(self, page, current_page_num, temp_images_dir):
    """从页面中提取图像
    
    Args:
        page: PyMuPDF页面对象
        current_page_num (int): 当前页码（1-based）
        temp_images_dir (str): 临时图像目录路径
        
    Returns:
        list[PdfImage]: 提取的图像列表
    """
    page_images = []
    
    # 提取图像
    images = page.get_images(full=True)
    for image_idx, img in enumerate(images):
        try:
            xref = img[0]
            base_image = page.parent.extract_image(xref)
            image_bytes = base_image['image']
            image_ext = base_image['ext']
            image_path = os.path.join(temp_images_dir, f'page_{current_page_num}_image_{image_idx}.{image_ext}')
            
            # 保存图像
            with open(image_path, 'wb') as f:
                f.write(image_bytes)
            
            # 获取图像实际边界框
            image_rects = page.get_image_rects(xref)
            if image_rects:
                # 使用第一个边界框（通常每个图像只有一个边界框）
                rect = image_rects[0]
                bbox = (rect.x0, rect.y0, rect.x1, rect.y1)
                logger.info(f"获取图像边界框: {bbox}")
            else:
                # 如果没有获取到边界框，使用默认值
                bbox = (0, 0, 100, 100)
                logger.warning(f"无法获取图像边界框，使用默认值: {bbox}")
            
            # 直接创建PdfImage对象并添加到列表
            pdf_image = PdfImage(
                page_num=current_page_num,
                image_idx=image_idx,
                image_path=image_path,
                bbox=bbox
            )
            page_images.append(pdf_image)
            logger.info(f"提取图像: 第{current_page_num}页-图像{image_idx}, 保存到: {image_path}, 边界框: {bbox}, 位置: x0={bbox[0]}, y0={bbox[1]}, x1={bbox[2]}, y1={bbox[3]}")
        except Exception as e:
            logger.error(f"提取图像时出错: {str(e)}")
    
    return page_images
```

### 3. 测试和验证

#### 3.1 运行测试
- 运行翻译任务，处理包含图像的PDF文件
- 检查日志中的图像边界框是否正常
- 验证生成的Word文档中图像位置是否正确

#### 3.2 验证要点
- 图像边界框格式是否正确（y0 < y1）
- 图像在Word文档中的位置是否正确
- 图像与文本的相对顺序是否正确
- 12页的图是否如原文一样放在"图1：智能体式AI问题解决流程"之前

## 预期效果

- 图像边界框格式正确，与文本块边界框格式一致
- 图像在Word文档中的位置正确
- 图像与文本的相对顺序正确
- 12页的图正确显示在"图1：智能体式AI问题解决流程"之前

## 影响范围

- 只影响图像处理部分，不会影响文本处理和其他功能
- 所有包含图像的PDF文档都会受益于这个修复
- 修复后图像在Word文档中的位置将更加准确