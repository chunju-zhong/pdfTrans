# 完善测试用例，添加 total_pages 属性测试

## 问题分析

根据今天的修改，我们添加了使用 `PdfExtractor` 的 `total_pages` 属性来获取总页数的功能，但测试文件中没有专门测试这个属性的测试用例。我需要添加一个测试用例来验证 `total_pages` 属性能够正确获取PDF文件的总页数。

## 计划内容

### 1. 添加 total_pages 属性测试用例

* **修改 tests/test_pdf_extractor.py 文件**：添加一个新的测试方法 `test_total_pages`，用于测试 `total_pages` 属性的功能

## 具体修改点

### tests/test_pdf_extractor.py

1. **在文件末尾添加测试方法**：添加一个新的测试方法 `test_total_pages`，验证 `total_pages` 属性能够正确获取PDF文件的总页数

## 预期结果

* 测试用例能够验证 `total_pages` 属性的功能
* 所有测试仍然通过
* 测试覆盖范围更加全面

