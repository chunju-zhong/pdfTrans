# 测试用例更新计划

## 1. 更新 `tests/test_app.py`

### 1.1 添加测试语义合并开关存在
- 在 `test_translate_form_elements` 方法中添加对 `semantic_merge` 表单元素的检查
- 验证表单中包含语义合并开关控件

### 1.2 测试语义合并参数传递
- 在 `test_translate_route_with_doc_type_glossary` 方法中添加 `semantic_merge` 参数
- 测试启用语义合并的情况

### 1.3 测试禁用语义合并参数
- 添加新的测试方法 `test_translate_route_without_semantic_merge`
- 测试禁用语义合并的情况

## 2. 更新 `tests/test_translation_service.py`

### 2.1 测试语义合并参数处理
- 添加新的测试方法 `test_process_translation_with_semantic_merge`
- 测试启用语义合并的情况

### 2.2 测试禁用语义合并参数
- 添加新的测试方法 `test_process_translation_without_semantic_merge`
- 测试禁用语义合并的情况

## 3. 验证其他测试文件

### 3.1 确认不需要更新的测试文件
- `tests/test_semantic_merge.py` 和 `tests/test_semantic_merge_extended.py` - 这些文件测试底层的语义合并功能，不需要更新
- `tests/test_page_range_parsing.py` - 与语义合并无关
- `tests/test_same_language_optimization.py` - 与语义合并无关
- `tests/test_progress.py` - 与语义合并无关
- `tests/test_pdf_generator.py` 和 `tests/test_docx_generator.py` - 与语义合并无关
- `tests/test_aiping_translator.py` 和 `tests/test_silicon_flow_translator.py` - 与语义合并无关
- `tests/test_pdf_extractor.py` - 与语义合并无关
- `tests/test_style_extraction.py` - 与语义合并无关
- `tests/test_simple_pdf_gen.py` 和 `tests/test_pdf_page_translation.py` - 与语义合并无关

## 4. 测试执行

### 4.1 运行更新后的测试
- 执行所有测试，确保更新后的测试用例通过
- 验证现有测试用例仍然通过
- 确认新功能正常工作

## 技术要点
- 使用 pytest 框架进行测试
- 模拟表单提交时包含语义合并参数
- 测试翻译服务处理语义合并参数的逻辑
- 确保测试覆盖启用和禁用语义合并的两种情况