# AI 开发进度记录

### 2026-06-18
- **当前状态**：已完成短文本未翻译检测缺失修复
- **已完成任务**：
  - 降低 aiping 翻译器温度参数：
    - `modules/aiping_translator.py` — `temperature` 从 `0.7` 改为 `0.1`，`top_p` 从 `0.8` 改为 `0.9`
    - 与 siliconflow 翻译器参数一致，减少 LLM 创造性输出
  - 新增智能未翻译检测函数：
    - `services/translation_service.py` 新增 `_is_translation_unchanged()` 方法
    - 识别 LLM 自行添加 `|||` 分隔符但未翻译的场景（剥离分隔符后逐段检查是否来自原文）
  - 升级两处翻译后检测逻辑：
    - `translate_original_block()` 和 `translate_merged_block()` 从 `==` 升级为调用新函数
    - 日志增强：输出结果前200字符便于排查
  - 编写单元测试：
    - `tests/test_translation_service.py` 新增 `TestIsTranslationUnchanged` 类
    - 5个测试用例，11/11 全部通过（含原有6个回归测试）
- **技术实现**：
  - 温度调整：`temperature=0.1` 使模型输出更确定性，减少"创造性解释"
  - 检测算法：快速路径（完全相同）+ `|||` 分隔符检测（re.split 剥离 + 子串/词集合匹配）
  - 日志增强：WARNING 中同时展示原文和结果前缀，便于快速定位问题
- **影响**：
  - 减少 LLM 自行添加 `|||` 等非预期格式符号的概率
  - 即使 LLM 仍添加 `|||`，检测函数也能识别未翻译并记录 WARNING
  - 不再出现"翻译结果与原文不同但实质未翻译"的漏检情况
- **遇到的问题**：
  - Spec 经历6轮迭代才获批准：初始分析误归因于代码表格识别 → 修正为 LLM 自身行为 → 用户要求深入研究内部原因 → 排除输入/代码/配置三方面可能 → 确认三因素叠加根因
- **后续计划**：
  - 实际运行验证温度调整对 `|||` 自行添加的抑制效果
  - 观察生产环境 WARNING 日志确认检测函数触发情况

### 2026-06-14
- **当前状态**：完成翻译提示词3项优化和换行符清理策略微调
- **已完成任务**：
  - 翻译提示词规则10扩展为"代码段保留与格式化"：
    - 增加代码格式化要求（保持缩进、空格、换行、对齐）
    - 增加代码特征识别说明（编程关键字和特殊符号）
    - 增加代码注释不翻译要求
  - 翻译提示词规则10特征描述修复：
    - 移除"连续的多行缩进文本"和"特殊缩进结构"（输入中不存在）
  - 翻译提示词规则14强化为"禁止元注释和原文输出"：
    - 增加"严禁输出原文内容"和"不保留原文"
  - 换行符清理策略微调：
    - 将 `\n` 替换从空字符串改为空格（`text.replace('\n', '')` → `text.replace('\n', ' ')`）
    - 涉及 `paddle_extractor.py`、`pdf_extractor.py`、`coordinate_utils.py` 三个文件
- **技术实现**：
  - 规则10扩展：多行结构描述（4个子项），使用 `**加粗**` 强调关键约束
  - 规则14强化：在原有元注释禁止基础上增加原文输出禁止
  - 换行符替换策略：`\n` → `' '` 而非 `''`，避免单词粘连（如 `LicensingYou` → `Licensing You`）
- **影响**：
  - 翻译提示词更精确，减少 LLM 误判代码段的可能性
  - 明确禁止翻译结果输出原文，避免英文原文残留
  - 换行符处理更合理，避免单词粘连问题
- **遇到的问题**：
  - 规则10中"{}"在 f-string 中需转义为"{{}}"
- **后续计划**：
  - 运行完整文档翻译验证提示词优化效果

### 2026-06-13
- **当前状态**：已完成换行符清理集中到提取阶段的改造
- **已完成任务**：
  - 重新定义了换行符处理策略：统一在提取阶段删除 `\n`，后续阶段不再处理换行符：
    - OCR 提取 `_build_text_from_textlines`：所有文本块中直接删除 `\n`（不再区分标题/正文）
    - PDF 提取 `_extract_text_blocks`：创建 TextBlock 后删除 `\n`
    - 表格单元格文本：OCR 和 PDF 模式创建 PdfCell 时删除 `\n`
  - 移除了其他阶段分散的换行符处理逻辑：
    - `_preprocess_text` 简化为 `return text.strip()`
    - `_postprocess_text` 简化为仅 `.strip()` + 空结果检查
    - 系统提示词删除"保持换行符一致性"规则（原第7条），后续规则重新编号
  - PDF 提取器中删除了 `_is_title_text` 和 `_clean_title_newlines` 方法（约90行）
  - 移除未使用的 `import re`（`_postprocess_text` 中不再需要）
  - 更新 CHANGELOG 和测试断言
- **技术实现**：
  - 提取阶段统一策略：`text.replace('\n', '')`，直接删除而非替换为空格
  - 移除翻译阶段的补偿处理：pre/post process 不再按行分割和重组
  - 移除 LLM 提示词中关于换行符保持的指令，减少 token 消耗
- **影响**：
  - 换行符处理逻辑更集中、可维护性更好
  - 表格和文本块提取后不再包含换行符，避免下游处理意外
  - 移除 ~90 行冗余代码（`pdf_extractor.py`）
  - 翻译阶段更简洁，提示词更轻量（少 1 条规则）
- **遇到的问题**：
  - 无重大问题，改造过程顺利
- **后续计划**：
  - 修复 code review 发现的 `import re` 死代码问题
  - 运行完整文档翻译验证整体效果

### 2026-06-12
- **当前状态**：已完成翻译提示词换行符保持规则和拆分算法换行符处理优化
- **已完成任务**：
  - 翻译提示词添加"保持换行符一致性"规则：
    - `modules/translator.py` `_generate_system_prompt` 方法新增第7条规则
    - 规则内容：原文有换行符则保留，原文无换行符则不添加，禁止自行调整换行符位置
    - 使用加粗格式提高大模型关注度
  - 拆分算法优化——以换行符为分隔点拆分：
    - `utils/text_processing.py` `split_translated_result` 函数添加换行符检测逻辑
    - 新增4个辅助函数：`_split_by_newlines`, `_split_by_newlines_and_ratio`, `_split_with_excess_newlines`, `_split_by_ratio`
    - 拆分策略：根据换行符数量与原始块数量关系选择最优策略
  - 测试用例添加：
    - `tests/test_newline_preservation.py`（14个测试用例）
    - `tests/test_split_by_newline.py`（9个测试用例）
- **技术实现**：
  - 翻译提示词规则：使用详细的4个子要点描述换行符保持规则
  - 拆分策略：换行符数量匹配时以换行符拆分，不匹配时采用混合策略
  - 辅助函数封装：将原有按比例拆分逻辑封装为独立函数
- **影响**：
  - 解决第15页截断问题，换行符正确分配到对应的原始文本块
  - 提高翻译结果与原文结构的对应性
- **遇到的问题**：
  - 发现翻译提示词规则虽然添加了，但大模型仍然保留了原文的换行符（因为原文本身就是两行布局）
  - 根本原因：拆分算法没有考虑换行符位置，导致换行符被分配到不应该包含换行符的文本块
- **后续计划**：
  - 验证修改后的效果，确保截断问题得到解决
  - 优化 `_split_by_ratio` 函数长度（当前155行，超过50行限制）

### 2026-06-11（续）
- **当前状态**：已完成文本提取时清理换行符功能，发现新问题
- **已完成任务**：
  - OCR提取时清理标题文本换行符：
    - `modules/ocr/paddle_extractor.py` 添加 `TITLE_LABELS` 常量
    - 在 `_build_text_from_textlines` 方法中清理标题换行符
  - 非OCR提取时清理标题文本换行符：
    - `modules/pdf_extractor.py` 添加 `_is_title_text` 方法（字体大小、文本长度、章节号判断）
    - 添加 `_clean_title_newlines` 方法（替换换行符为空格）
  - 添加标题识别逻辑和清理策略：
    - 清理策略：将 `\n` 替换为空格，清理多余空格
- **技术实现**：
  - 定义 `TITLE_LABELS` 常量：识别标题类型的布局标签
  - `_is_title_text` 方法：基于字体大小（>12pt）、文本长度（<50字符）、章节号模式判断
  - `_clean_title_newlines` 方法：替换 `\n` 为空格，清理多余空格
- **影响**：
  - 提高标题文本渲染质量，避免过度截断
- **遇到的问题**：
  - 发现新问题"拆分逻辑未清理标题换行符"，导致"2.4. 1 一般场地"换行
  - 根因：`split_translated_result` 函数未对拆分后的文本块进行标题识别和换行符清理
- **后续计划**：
  - 修复拆分逻辑中的换行符清理问题
  - 在 `split_translated_result` 函数中添加标题识别和换行符清理

### 2026-06-11
- **当前状态**：已完成第22页 Acknowledgments 跨段落合并语序混乱修复 + LLM 提示词语言无关优化
- **已完成任务**：
  - 第22页段落边界检测修复：
    - 新增 **示例F：段落边界** few-shot 示例（前段以句号结尾 + 后段以全新句子开头 + 话题转换）
    - 强化"进一步判断"规则：终结标点 + 新句子开头 + 新话题/主语
    - 单对版本和批量版本同步更新
  - 移除"分段长度平衡调整"步骤：
    - `utils/text_processing.py` 移除 L660-709 代码块
    - 根因：该逻辑从最后一个块尾部提取文本均匀分配到前面各块，破坏语序
  - LLM 提示词语言无关优化：
    - 签名行/列表项特征改为通用描述（不再依赖 `—`/`•`/`-` 等特定符号）
    - few-shot 示例理由改为通用描述（不再引用"大写字母/小写字母"）
    - [进一步判断]规则改为语言无关（"独立的新主语/新话题"替代"大写字母开头"）
  - 提示词重构（顺序块列表模式）：
    - 输入从成对文本对改为顺序块列表（块1, 块2, ..., 块N），每块只出现一次
    - 输出要求改为 N-1 个合并判断（merge 数组长度为块数减1）
    - 新增「上下文感知指导」章节：指示 LLM 判断块i与块i+1时参考块1到块i-1的语义角色和内容
  - 接口签名变更：
    - `SemanticAnalyzer.batch_analyze_semantic_relationship(blocks, source_lang)` 返回 `len(blocks)-1` 个决策
    - `AipingSemanticAnalyzer` 同步适配新接口
    - `blocks` 长度 ≤ 1 时返回空列表
  - 并行分批策略重构（`parallel_batch_analyze`）：
    - 实现重叠1块分批：批次0取 blocks[0:batch_size]，批次k取 blocks[prev_last_idx:prev_last_idx+batch_size]
    - 每批包含前批最后1块作为上下文重叠，N块输出N-1个判断
    - 合并时直接拼接无需丢弃，验证：25块分3批 → 9+9+6=24 ✓
  - 调用方适配：
    - `merge_semantic_blocks_with_llm_two_phase` 构建 `block_texts` 列表而非 `text_pairs`
    - `merge_semantic_blocks_with_llm` 构建 `batch_block_texts` 列表而非 `batch_text_pairs`
  - 测试适配：
    - 3个测试文件适配新接口，25个测试全部通过
    - 新增上下文感知提示词断言验证
- **技术实现**：
  - 核心变换：N个顺序块 → N-1个相邻对合并判断
  - 重叠块策略：每批（除第一批外）包含前批最后1块，确保批次边界处上下文连续
  - 上下文感知：LLM 能参考前文语义角色（如签名行标志引用结束）做出更准确判断
- **影响**：
  - 消除重复文本，节省 token（4块从8次出现改为4次）
  - LLM 能利用前文上下文判断合并，提升准确性
  - 批次边界处合并判断正确（重叠块提供上下文）
- **遇到的问题**：
  - 无重大问题，重构过程顺利
- **后续计划**：
  - 验证完整文档翻译效果
  - 观察上下文感知对合并准确性的实际提升

### 2026-06-10
- **当前状态**：已完成 PDF 翻译中对齐方式文本块语序混乱问题的全面修复（LLM 提示词改进 + 对齐检测 + bbox 越界修复）
- **已完成任务**：
  - 文本块对齐方式属性与检测：
    - `TextBlock` 新增 `alignment` 属性（0=左, 1=中, 2=右），支持序列化
    - 新增 `detect_text_block_alignment()` 函数，左/右对齐优先于居中检测（阈值15%）
    - 非 OCR 和 OCR 提取路径均已集成对齐检测
  - 语义合并对齐检查：
    - 三个语义合并函数均增加 `is_different_alignment` 检查，不同对齐方式的块不合并
    - PDF 渲染使用提取到的 alignment 值替代硬编码
  - LLM 语义分析提示词完全重写：
    - 两步分析法：语义角色识别（5种角色）+ 合并决策矩阵
    - 5 个 few-shot 边界场景示例覆盖签名行/引用/列表/标题所有边界类型
    - 单对版本和批量版本同步更新，AipingSemanticAnalyzer 自动继承
    - 第3页 Praise 书评页验证通过：7个文本对决策全部正确
  - 拆分后 bbox 越界修复：
    - `translation_service.py` 增加 `min(x0+max_width, original_x1)` 保护
    - Madhav 引用末句 "涵盖了该领域..." 不再超出页面右边界
  - 保留原文格式：
    - `TextBlock.__init__` 移除 `text.strip()`，保留换行和空格
- **技术实现**：
  - 对齐检测算法：`left_ratio = x0/page_width < 15% → 左对齐`，`right_ratio = (width-x1)/page_width < 15% → 右对齐`，否则居中
  - LLM 两步分析：Step1 判断每个文本的语义角色 → Step2 基于 (role1, role2) 决策矩阵输出 merge=true/false
  - 决策矩阵核心规则：(signature, *) = false, (*, signature) = false, (title, body) = false, (list_item, list_item) = false, (body, body) = true, (quote_body, quote_body) = true
- **影响**：
  - 左右对称布局（如 Praise 书评页）的左对齐标题和右对齐正文不再被错误合并
  - 签名行（如 "—Jay Alammar"）不再与下一个引用的正文跨引用合并
  - 同一引用内的句子延续仍正确合并
  - 拆分后的短文本块不再因 x1 越界被截断或显示位置异常
  - 原文中的换行和空格在后续处理流程中被完整保留
- **遇到的问题**：
  - 初期尝试硬编码签名行检测规则（NON_NAME_STARTERS 黑名单 + ROLE_KEYWORDS 词表），用户指出无法区分签名行与列表项/标题行，最终转向改进 LLM 提示词方案
  - 居中检测优先级问题：初始实现居中检测优先于左右对齐，导致左右对称布局中文本全被误判为居中，修正为左/右优先
  - 拆分 bbox 越界根因隐蔽：`translation_service.py` 的 `x0 + max_width` 计算未考虑 x0 较大的块
- **后续计划**：
  - 验证第2页右对齐文本翻译语序正确性
  - 运行完整300+页文档翻译验证全局效果
  - 考虑改进 `split_translated_result()` 的跨语言字符长度比例不一致问题（当前按英文比例分配中文字符）

### 2026-06-09（续2）
- **当前状态**：已修复罗马数字页码识别、合并块 bbox 高度、CJK 行高、PDF 字体放大、翻译文本按比例分配等5个问题
- **已完成任务**：
  - 修复罗马数字页码未被识别为页脚导致跨页文本错误合并：
    - `text_analyzer.py` 新增 `roman_to_int()` 函数和 `ROMAN_NUMERAL_PATTERN` 正则
    - `identify_page_numbers()` 新增罗马数字页码识别（位置/字体/递增模式/值匹配）
    - 新增14个单元测试，全部35个测试通过
  - 修复合并块拆分后 bbox 高度过大导致文本溢出和段落遮挡：
    - `translation_service.py` 拆分块 bbox 高度改为使用各块自身的 `original_bbox[3]`
  - 修复 CJK 字体行高大于拉丁字体导致翻译文本溢出 bbox：
    - `pdf_generator.py` 新增原文行高倍率计算，所有 `insert_textbox()` 添加 `lineheight` 参数
  - 修复 PDF 生成器首次渲染使用放大字体导致文本溢出：
    - `pdf_generator.py` 首次尝试使用原始字体大小，溢出时逐步缩小
  - 修复合并块拆分时均等分配导致第二个块为空、翻译文本丢失：
    - `split_translated_result()` 分配策略从均等分配改为按原始文本长度比例分配
    - 新增保护：非最后一个块不消耗全部剩余文本
- **技术实现**：
  - 罗马数字识别：位置（顶部/底部15%）+ 字体（<10.0）+ 递增模式 + 值匹配
  - 行高倍率计算：`bbox高度 / (字体大小 × 估算行数)`，下限1.0，默认1.2
  - 按比例分配：`target_len = round(remaining_len × original_lengths[i] / remaining_original_len)`
  - 保护机制：`actual_end ≤ translation_len - min_chars × remaining_blocks`
- **影响**：
  - 罗马数字页码不再被误认为正文，跨页文本不再错误合并
  - 翻译文本行高与原文一致，不再溢出 bbox
  - 合并块拆分后各块分到与其原始长度相当的翻译文本，不再出现空块
- **遇到的问题**：
  - 最初认为文本丢失是 `adjust_split_position` 推进过多导致，实际根因是均等分配策略
  - CJK 行高问题最初用固定 `lineheight=1.2` 修复，后改为基于原文行高计算
- **后续计划**：
  - 验证300+页文档翻译质量
  - 修复表格标题/脚注被错误合并进单元格

### 2026-06-09（续）

### 2026-06-09
- **当前状态**：已完成 Word 表格尺寸匹配原文、PDF/Word 表格对齐方式匹配原文
- **已完成任务**：
  - Word 表格尺寸匹配原文：
    - `docx_generator.py` `_add_table()` 重写：使用 `PdfTable.col_widths`/`row_heights`/`bbox` 设置表格尺寸
    - 新增 `_resolve_col_widths()` 和 `_resolve_row_heights()` 辅助方法
    - 合并单元格宽度/高度按跨列/行数等分分配
    - 表格布局 `tblLayout=fixed`，通过 `tblGrid`/`gridCol` 精确控制列宽
  - PDF 和 Word 表格对齐方式匹配原文：
    - `PdfCell` 新增 `alignment` 参数，`PdfTable` 新增 `alignment` 参数
    - 新增 `extract_cell_alignment()` 从字符 bbox 位置推断单元格对齐
    - 新增 `extract_table_alignment()` 从表格 bbox 位置推断表格整体对齐
    - `table_processor.py` 提取时从字符级 bbox 计算文本 bbox 并推断对齐
    - `paddle_extractor.py` OCR 模式新增对齐提取
    - `pdf_generator.py` 和 `docx_generator.py` 使用提取的对齐信息
    - 确认 PyMuPDF 无原生对齐属性，对齐完全由字符位置推断
- **技术实现**：
  - 单位转换：1 PDF point = 12700 EMU, 1 inch = 1440 twips, 1 twip = 635 EMU
  - 对齐推断阈值：中心偏移 < 15% → 居中，左偏移 < 10% → 左对齐，右偏移 < 10% → 右对齐
  - 表格对齐推断：表格中心与页面中心偏移 < 5% → 居中
  - 合并单元格宽度分配：`cell_width / col_span` 等分到各列
- **影响**：
  - Word 表格列宽/行高/整体宽度与原文 PDF 一致
  - PDF 和 Word 表格单元格对齐方式与原文一致
  - 表格整体对齐方式与原文一致
- **遇到的问题**：
  - 代码审查发现合并单元格宽度/高度未分配到跨列/行，已修复
  - 子代理未正确应用 docx_generator 对齐变更，手动修复
- **后续计划**：
  - 实现 Markdown 合并单元格占位符保护机制
  - 修复表格标题/脚注被错误合并进单元格
  - 修复 `test_parse_simple_table` 预存测试失败（`_TableHtmlParser` 返回元组但测试期望字符串）

### 2026-06-09（前序）
- **当前状态**：已修复 Rect.intersect() 原地修改 bug，Word 合并单元格已支持，Markdown 合并单元格暂未启用
- **已完成任务**：
  - 修复 `Rect.intersect()` 原地修改导致表格文本重叠检测失效：
    - PyMuPDF 的 `Rect.intersect()` 会原地修改调用者矩形，循环中累积重叠面积计算全部错误
    - 3 个文件 5 处 `.intersect()` 改为 `&` 运算符
    - 新增累积重叠面积检测和空列表回退
    - 新增 `table_bbox` 参数过滤表外字符
  - Word 合并单元格支持：
    - `docx_generator.py` `_add_table()` 对 `row_span > 1` 或 `col_span > 1` 的单元格调用 `cell.merge()`
    - 跳过 None 位置，合并单元格字号按 span 缩放
  - Markdown 合并单元格诊断：
    - 发现 `_format_with_layout_model` 会将 HTML 表格发给 LLM 重新格式化，LLM 将 HTML 转回 pipe 格式
    - 需要实现占位符保护机制后再启用
  - 修复 `markdown_generator.py` 中 `\$` 无效转义序列的 SyntaxWarning
  - 新增 `tests/test_rect_intersect_fix.py`：13 个测试用例
- **技术实现**：
  - `Rect.intersect()` vs `&` 运算符：`r1.intersect(r2)` 原地修改 r1 并返回 r1，`r1 & r2` 返回新矩形不修改 r1
  - 累积重叠面积：遍历所有单元格计算交集面积总和，超过文本块面积 50% 才标记为表格文本
  - 表外字符过滤：`extract_table_cells_by_bbox` 新增 `table_bbox` 参数，过滤中心点超出表格 bbox 的字符
- **影响**：
  - PDF 第22页表格最后一列不再有重复文字
  - Word 输出正确合并单元格
  - Markdown 合并单元格暂未生效（仍为 pipe 格式）
- **遇到的问题**：
  - 多次尝试修改重叠检测逻辑无效，最终通过诊断日志发现 `Rect.intersect()` 原地修改是根因
  - Markdown HTML 表格被 LLM 覆盖，需要占位符保护机制（类似公式保护），暂未实现
- **后续计划**：
  - 实现 Markdown 合并单元格占位符保护机制
  - 修复表格标题/脚注被错误合并进单元格

### 2026-06-08
- **当前状态**：已完成合并单元格渲染优化和代码审查优化
- **已完成任务**：
  - 合并单元格渲染优化——解决文字显示不全和线条分隔问题：
    - `PdfCell` 新增 `row_span`/`col_span` 字段，支持序列化
    - OCR 模式：解析 HTML rowspan/colspan，展开为完整二维矩阵
    - PyMuPDF 模式：新增 `compute_span_from_none_positions()` 从 None 分布推断 span
    - PDF 生成：合并单元格使用完整 bbox 渲染，网格线按可见段绘制
    - 列宽按比例缩放，修复横线超出表格边界
  - 合并单元格遮挡逻辑精确化：
    - 水平线遮挡只排除 col_span>1 的上/下边框
    - 垂直线遮挡只排除 row_span>1 的左/右边框
  - 代码审查优化：
    - 简化 span 计算第三步验证逻辑
    - 提取 `_get_cell_span()` 辅助函数
    - 调试日志从 info 降级为 debug
- **技术实现**：
  - 合并单元格检测：PyMuPDF 在被合并位置返回 None，通过 None 分布推断 span；OCR 模式通过 HTML 属性获取 span
  - 网格线遮挡：基于 span 信息计算可见线段，遮挡合并单元格内部线条，保留跨行跨列边框
  - 列宽缩放：`sum(col_widths)` 可能超过 `table_width`（合并单元格等分分配导致），按比例缩放修正
- **影响**：
  - 合并单元格文字完整显示，不再被截断
  - 合并单元格内部无多余线条
  - 表格横线不再超出表格右边界
  - 生产日志更干净（调试信息降级为 debug）
- **遇到的问题**：
  - `compute_span_from_none_positions` 多次迭代修复：初始版本将同行 col_span 产生的 None 误归为上方单元格的 row_span，导致普通单元格被误判为合并单元格，遮挡了正常横线
  - 遮挡边界排除逻辑过于激进：初始版本排除所有合并单元格边框，导致正常横线被错误取消
- **后续计划**：
  - 修复表格标题/脚注被错误合并进单元格
  - 运行完整文档翻译验证合并单元格效果

### 2026-06-07
- **当前状态**：已完成非OCR模式表格精准还原和表格翻译分隔符丢失修复
- **已完成任务**：
  - 非 OCR 模式表格精准还原——使用 PyMuPDF 真实单元格 bbox 替代均匀分割：
    - 新增 `_build_bbox_matrix()` 函数，将 `rows_data` 转为行×列 bbox 矩阵
    - 新增 `calculate_row_heights_from_bboxes()` 和 `calculate_col_widths_from_bboxes()` 函数
    - 修改 `extract_table_cells_by_bbox` 返回值新增 `rows_data`
    - 修改 `extract_tables_by_pymupdf` 优先使用真实 bbox，均匀分割仅作 fallback
  - 修复表格行翻译分隔符丢失导致单元格内容重复：
    - 在 `translator.py` system prompt 新增第17条规则：保留 "|||" 分隔符
    - 在 `translate_table_row` 中增加分隔符数量不匹配的 fallback
  - OCR 模式表格网格布局重构：
    - 重构 `_compute_table_grid()` 使用列边界聚类替代均匀分割
    - 新增 `_cluster_1d()` 一维聚类函数
- **技术实现**：
  - 非 OCR 模式：PyMuPDF 的 `table.rows[i].cells[j]` 提供真实单元格 bbox（含合并单元格信息），之前被 `calculate_cell_bbox` 均匀分割完全替代
  - 翻译分隔符：`translate_table_row` 用 `\n|||` 拼接同行单元格后 LLM 未保留分隔符，导致拆分后内容错位
  - OCR 模式：列边界聚类替代均匀分割，解决 OCR textline 位置不均匀导致的列宽不准
- **影响**：
  - 非 OCR 模式表格还原更精准，合并单元格和不等行高列宽正确显示
  - 表格翻译不再出现短文本单元格（如 "Output"）被长文本内容覆盖的问题
  - OCR 模式表格行列对齐更准确
- **遇到的问题**：
  - 发现表格标题/脚注（如 "Table 5. An example of role prompting"）被 PyMuPDF 的 `table.rows` 最后一行单元格 bbox 覆盖，导致合并进单元格——尚未修复，已加入 TODO
- **后续计划**：
  - 实现表格标题/脚注后处理分离方案（`separate_table_caption_and_footnote`）
  - 运行程序验证第20页表格 Output 单元格翻译效果

### 2026-06-06
- **当前状态**：已修复日期型页尾未被识别导致跨页错误合并问题
- **已完成任务**：
  - 修复日期型页尾未被识别导致跨页错误合并：
    - 修改 `text_analyzer.py` `_add_similar_blocks()` 短文本相似度阈值从 1.0（完全相同）降低到 0.8
    - 统一简化分级阈值（短文本 1.0 / 长短混合 0.95 / 长文本 0.9）为单一 0.8 阈值
    - 根因：原短文本阈值 1.0 过于严格，"2025年2月 8" 与 "2025年2月 9" 的 LCS 相似度 ≈ 88.9%，低于 100% 阈值，无法被识别为页脚
    - 验证："2025年2月 8" vs "2025年2月 9" 相似度 0.8889 > 0.8 ✓，"xii | Foreword" vs "xiv | Foreword" 相似度 0.9286 > 0.8 ✓
  - 修复文本框超出原文宽度/页宽度和 tight bbox 高度过大问题
  - 修复文本框超出原文宽度和页宽度：
    - 修复 `pdf_generator.py` 溢出重试逻辑：扩展文本框时限制右/下边界不超过页面宽高
    - 修复 `paddle_extractor.py` `_compute_tight_bbox()` 宽度容差从 30% 降低到 10%
    - 增加 tight bbox 页面宽度限制，防止 OCR 检测噪声导致 bbox 超出页面
  - 修复 tight bbox 高度过大：
    - 在 `_compute_tight_bbox()` 中增加高度 10% 容差限制（与宽度对称）
    - 新增 `_estimate_font_size_from_textlines()` 辅助方法，使用 textline 平均高度估算 font_size
    - 修复 5 个非 TEXT_LABELS 路径的 font_size 估算，从 `bbox_height * 0.75` 改为 textline 平均高度 * 0.75
- **技术实现**：
  - 根因分析：非语义合并路径下，PDF 生成器溢出扩展无页面宽度限制 + OCR tight bbox 宽度容差过大（30%）+ 高度无容差限制 + font_size 估算使用整个 bbox 高度（含行间距）
  - 修复策略：页面边界限制 + 容差收紧 + textline 级别精确估算
- **影响**：
  - 文本框不再超出页面边界，翻译文本渲染更精确
  - font_size 估算更准确，白色背景 padding 更合理
  - 多行文本块的 bbox 高度不再包含过多行间距
- **遇到的问题**：
  - 初始分析误判为语义合并路径的 bbox 扩展问题，用户指出未使用语义合并后重新定位根因
- **后续计划**：
  - 实际运行验证第24页文本框宽度问题是否解决

### 2026-06-04
- **当前状态**：已完成OCR提取引擎完善、代码审查修复和文本块合并高度计算bug修复
- **已完成任务**：
  - 实现OCR提取引擎完善和代码审查修复：
    - 新增 `tests/test_ocr_worker.py`，覆盖OCR工作器异常类型、心跳发送、参数降级、子进程重试逻辑
    - 新增 `tests/test_system_profiler.py`，覆盖系统资源检测、5个内存层级参数计算、高负载参数缩减
    - 新增 `tests/test_omml_constants.py` 和 `tests/test_table_grid.py`
    - 重构 `modules/ocr/ocr_worker.py`，增强子进程管理、心跳监控、动态超时和参数降级重试
    - 重构 `modules/ocr/paddle_extractor.py`，增强LaTeX清洗、figure_caption文本提取、表格HTML提取修正
    - 新增 `modules/ocr/system_profiler.py`，实现系统资源自适应参数计算
    - 修复 `utils/text_processing.py` 中文本块合并高度计算错误
    - 更新多个模块集成OCR功能
  - 上传OCR需求文档和PRP文件：
    - 新增 `docs/OCR-requiremnt.md`，定义三阶段OCR功能规划
    - 新增OCR提取功能实施计划和产品需求文档
    - 新增 `tests/test_ocr_extractor.py`，OCR提取器测试用例
  - 实现OCR模式改进、安全修复与代码质量提升：
    - 创建 `modules/ocr/` 包，包含OCR引擎架构（base.py、factory.py、__init__.py）
    - 实现PaddleOCR提取器核心（分步加载策略、GPU自动检测、内存检查、日志恢复、字体估算、精确边界框、表格网格布局）
    - 实现OCR子进程工作器（独立子进程、心跳监控、动态超时、参数降级重试）
    - 实现系统资源自适应参数（5个内存层级、高负载自动降级）
    - 扩展数据模型（序列化方法、PdfTable新字段）
    - 安全修复（SECRET_KEY硬编码移除、文件名验证、页码范围限制）
    - 代码质量提升（翻译回退、字体估算修正、资源释放保护、numpy数组真值检查修复）
- **技术实现**：
  - 基于PP-StructureV3实现分步加载策略，避免2.5GB+内存溢出
  - 子进程spawn模式运行OCR，心跳监控和参数降级确保稳定性
  - 5个内存层级自适应参数计算，适配不同硬件环境
  - 修正 `merge_semantic_blocks` 中高度计算的逻辑错误
- **影响**：
  - 完整的OCR提取引擎支持扫描版PDF文档翻译
  - 安全性显著增强，防止路径遍历和DoS攻击
  - 代码质量提升，翻译回退和资源释放更加健壮
- **遇到的问题**：
  - PPStructureV3构造会破坏root logger配置，需在构造后自动恢复
  - 多行文本块字体估算过大问题，需三级策略修正
- **后续计划**：
  - 进一步优化术语提取的准确性和效率
  - 完善测试用例，提高测试覆盖率
  - 优化用户界面，提供更好的用户体验
