## 问题分析

**现象**：当前在批量语义分析失败时，会回退到单对分析，处理每个块。

**用户要求**：在批量语义分析失败时，不回退到单对分析。

**当前实现**：
```python
except Exception as e:
    logger.error(f"批量语义分析失败: {str(e)}")
    # 分析失败时，回退到单对分析
    logger.info(f"回退到单对分析，处理 {len(batch_blocks)} 个块")
    for j, curr_block in enumerate(batch_blocks):
        # 检查当前块是否延续前一个句子
        is_continuation = _is_sentence_continuation(curr_block.block_text)
        prev_merged_text = current_merged.block_text
        prev_ends_with_sentence = prev_merged_text.strip().endswith(('.', '!', '?', '。', '！', '？'))
        prev_ends_with_space = prev_merged_text.endswith(' ')
        
        # 增强的回退机制：特别检查句子延续的情况
        should_merge_fallback = False
        if not prev_ends_with_sentence:
            # 检查前一个块是否为图片注释，如果是，不合并
            if is_image_caption(prev_merged_text):
                logger.info(f"回退机制：前一个块是图片注释，不合并，块1='{prev_merged_text[:50]}...'，块2='{curr_block.block_text[:50]}...'")
            elif prev_ends_with_space:
                # 前一个块以空格结尾且不是完整句子，很可能是句子延续
                should_merge_fallback = True
                logger.info(f"回退机制：前一个块以空格结尾且不是完整句子，块1='{prev_merged_text[:50]}...'，块2='{curr_block.block_text[:50]}...'，应该合并")
            elif curr_block.block_text.strip() and not curr_block.block_text.strip().startswith(('$', '#', '@', 'http://', 'https://')):
                # 前一个块不是完整句子，当前块不是特殊标记或链接，可能是句子延续
                should_merge_fallback = True
                logger.info(f"回退机制：前一个块不是完整句子，块1='{prev_merged_text[:50]}...'，块2='{curr_block.block_text[:50]}...'，应该合并")

        if (not prev_ends_with_sentence and is_continuation) or should_merge_fallback:
            # 合并块，处理空格
            prev_text = current_merged.block_text
            curr_text = curr_block.block_text

            if prev_text.endswith(' ') and curr_text.startswith(' '):
                current_merged.block_text = prev_text + curr_text[1:]
            elif not prev_text.endswith(' ') and not curr_text.startswith(' '):
                current_merged.block_text = prev_text + ' ' + curr_text
            else:
                current_merged.block_text = prev_text + curr_text

            current_merged.original_blocks.append(curr_block)

            curr_bbox = curr_block.block_bbox
            curr_width = curr_bbox[2] - curr_bbox[0] if len(curr_bbox) >= 4 else 0
            curr_height = curr_bbox[3] - curr_bbox[1] if len(curr_bbox) >= 4 else 0

            current_merged.max_width = max(current_merged.max_width, curr_width)
            current_merged.max_height = max(current_merged.max_height, curr_height)
            
            logger.info(f"回退机制：合并块成功，合并后文本长度={len(current_merged.block_text)}, 包含 {len(current_merged.original_blocks)} 个原始块")
        else:
            # 保存当前合并块
            merged_blocks.append(current_merged)
            block_mapping.append(current_merged.original_blocks)
            logger.info(f"回退机制：保存当前合并块，文本='{current_merged.block_text[:100]}...', 包含 {len(current_merged.original_blocks)} 个原始块")

            # 开始新的合并块
            curr_bbox = curr_block.block_bbox
            curr_width = curr_bbox[2] - curr_bbox[0] if len(curr_bbox) >= 4 else 0
            curr_height = curr_bbox[3] - curr_bbox[1] if len(curr_bbox) >= 4 else 0

            current_merged = MergedBlock(
                block_text=curr_block.block_text,
                original_blocks=[curr_block],
                max_width=curr_width,
                max_height=curr_height
            )
            
            logger.info(f"回退机制：开始新合并块，文本='{current_merged.block_text[:100]}...'")
            
            # 剩余的块重新开始批次处理
            break
```

## 解决方案

**修改目标**：移除批量语义分析失败时的回退到单对分析逻辑。

**修改计划**：

### 1. 移除回退逻辑
- 删除 `except` 块中的回退到单对分析的代码
- 保留错误日志记录
- 在异常处理中直接跳过当前批次的处理，继续处理下一个批次

### 2. 异常处理优化
- 增强错误日志记录，确保错误信息清晰
- 确保异常处理不会影响整个语义合并过程

### 3. 代码清理
- 移除不再使用的变量和逻辑
- 确保代码结构清晰

## 预期效果

**技术效果**：
- 批量语义分析失败时，不再回退到单对分析
- 异常处理更加简洁，直接跳过失败的批次
- 代码逻辑更加清晰，易于理解和维护

**业务效果**：
- 处理流程更加一致，不会在失败时切换到不同的处理逻辑
- 减少了代码复杂度，降低了潜在的错误风险
- 保持了批量分析的设计初衷，即使在失败时也不回退到单对分析

## 验证方法

1. **运行测试套件**：运行完整的测试套件，确保所有测试用例都能通过
2. **模拟异常**：模拟批量语义分析失败的情况，确保不再回退到单对分析
3. **实际使用测试**：使用实际的PDF文件进行翻译测试，验证翻译质量

## 风险评估

**低风险**：
- 修改范围局限在异常处理块内，不影响其他功能
- 移除的是回退逻辑，不会影响正常的批量分析流程
- 增强了错误日志记录，便于调试和问题定位

**需要注意的事项**：
- 确保异常处理不会影响整个语义合并过程
- 确保错误日志记录足够清晰，便于问题定位
- 确保与现有代码的兼容性

## 实现修改点

**主要修改文件**：`utils/text_processing.py`

**修改函数**：`merge_semantic_blocks_with_llm`

**具体修改**：
1. 移除 `except` 块中的回退到单对分析的代码
2. 保留错误日志记录
3. 在异常处理中直接跳过当前批次的处理，继续处理下一个批次