## 问题分析

**现象**：用户要求移除硬编码的段落开头和图片块判断逻辑，以及相关的处理代码。具体需要移除的代码包括：

1. **特殊情况判断代码**（`/Users/chunju/work/pdfTrans/utils/text_processing.py#L684-689`）：
   ```python
   # 先检查是否为图片注释或新段落开始
   if is_image_caption(prev_text) or is_new_paragraph_start(curr_text):
       # 不合并，跳过批量分析
       logger.info(f"检测到特殊情况，跳过批量分析: 前一个块是图片注释={is_image_caption(prev_text)}, 当前块是新段落开始={is_new_paragraph_start(curr_text)}")
       break
   ```

2. **特殊情况处理代码**（`/Users/chunju/work/pdfTrans/utils/text_processing.py#L784-811`）：
   ```python
   else:
       # 处理特殊情况（图片注释或新段落开始）
       if i < total_blocks:
           curr_block = text_blocks[i]
           prev_text = current_merged.block_text
           curr_text = curr_block.block_text
           
           logger.info(f"特殊情况处理：不合并块，块1='{prev_text[:50]}...'，块2='{curr_text[:50]}...'")
           # 保存当前合并块
           merged_blocks.append(current_merged)
           block_mapping.append(current_merged.original_blocks)
           logger.info(f"保存当前合并块，包含 {len(current_merged.original_blocks)} 个原始块")

           # 开始新的合并块
           curr_bbox = curr_block.block_bbox
           curr_width = curr_bbox[2] - curr_bbox[0] if len(curr_bbox) >= 4 else 0
           curr_height = curr_bbox[3] - curr_bbox[1] if len(curr_bbox) >= 4 else 0

           current_merged = MergedBlock(
               block_text=curr_block.block_text,
               original_blocks=[curr_block],
               max_width=curr_width,
               max_height=curr_height
           )
           
           logger.info(f"开始新合并块，文本='{current_merged.block_text[:100]}...'")
           i += 1
   ```

3. **相关函数定义**：
   - `is_image_caption` 函数：使用正则表达式匹配 "图1："、"表2：" 等格式的图片/表格注释
   - `is_new_paragraph_start` 函数：使用硬编码的引导性短语列表来判断新段落开始

## 解决方案

**修改目标**：移除所有硬编码的段落开头和图片块判断逻辑，以及相关的处理代码，简化代码结构。

**修改计划**：

### 1. 移除特殊情况判断逻辑

* 删除 `merge_semantic_blocks_with_llm` 函数中的特殊情况判断代码（第684-689行）
* 不再检查是否为图片注释或新段落开始

### 2. 移除特殊情况处理代码

* 删除 `merge_semantic_blocks_with_llm` 函数中的特殊情况处理代码（第784-811行）
* 移除 `else` 分支，直接处理所有文本块

### 3. 移除相关函数定义

* 删除 `is_image_caption` 函数定义
* 删除 `is_new_paragraph_start` 函数定义

### 4. 简化文本对构建逻辑

* 直接构建相邻原始块的文本对，不进行任何特殊情况判断
* 保持批量分析的效率优势

### 5. 简化异常处理

* 确保异常处理逻辑与简化后的代码结构一致
* 保持代码结构清晰

## 预期效果

**技术效果**：

* 移除所有硬编码的段落开头和图片块判断逻辑
* 代码逻辑更加简洁，易于理解和维护
* 保持批量分析的效率优势
* 统一处理所有文本块，不再区分特殊情况

**业务效果**：

* 处理流程更加一致，不会在特殊情况下跳过批量分析
* 减少了代码复杂度，降低了潜在的错误风险
* 保持了批量分析的设计初衷，统一处理所有文本块

## 验证方法

1. **运行测试套件**：运行完整的测试套件，确保所有测试用例都能通过
2. **实际使用测试**：使用包含各种文本结构的 PDF 文件进行翻译测试
3. **代码审查**：确保所有硬编码判断逻辑和相关代码都已移除

## 风险评估

**低风险**：

* 修改范围局限在语义分析逻辑中，不影响其他功能
* 保持了批量分析的效率优势
* 减少了代码复杂度，降低了潜在的错误风险

**需要注意的事项**：

* 确保修改不会影响批量分析的性能
* 确保与现有代码的兼容性
* 确保移除的函数没有在其他地方被调用

## 实现修改点

**主要修改文件**：

1. `utils/text_processing.py`：
   * 修改 `merge_semantic_blocks_with_llm` 函数，移除特殊情况判断和处理代码
   * 删除 `is_image_caption` 和 `is_new_paragraph_start` 函数定义

**具体修改**：

1. 移除 `merge_semantic_blocks_with_llm` 函数中的特殊情况判断代码（第684-689行）
2. 移除 `merge_semantic_blocks_with_llm` 函数中的特殊情况处理代码（第784-811行）
3. 删除 `is_image_caption` 和 `is_new_paragraph_start` 函数定义
4. 简化文本对构建逻辑，直接构建相邻原始块的文本对
5. 确保代码结构清晰，易于理解和维护