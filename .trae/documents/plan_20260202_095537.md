# 移除 extract_page_text 方法并替换为 extract_text 方法

## 问题分析

根据用户要求，`extract_page_text` 方法不再需要，可以使用 `extract_text` 方法代替。我需要：

1. 移除 `modules/pdf_extractor.py` 中的 `extract_page_text` 方法
2. 替换所有调用 `extract_page_text` 的地方，改用 `extract_text` 方法

## 计划内容

### 1. 移除 extract_page_text 方法

* **修改 modules/pdf_extractor.py 文件**：删除 `extract_page_text` 方法的定义

### 2. 更新测试文件

* **修改 tests/test_pdf_extractor.py 文件**：
  - 移除 `test_extract_page_text` 测试方法
  - 移除 `test_extract_page_text_invalid_page` 测试方法
  - 移除 `test_extract_page_text_invalid_file` 测试方法

* **修改 tests/test_style_extraction.py 文件**：
  - 将 `pdf_extractor.extract_page_text(1)` 替换为 `pdf_extractor.extract_text(pages=[1]).pages[0]`

## 具体修改点

### modules/pdf_extractor.py

1. **移除第415-543行**：删除 `extract_page_text` 方法的完整定义

### tests/test_pdf_extractor.py

1. **移除第56-81行**：删除 `test_extract_page_text` 测试方法
2. **移除第83-100行**：删除 `test_extract_page_text_invalid_page` 测试方法
3. **移除第102-109行**：删除 `test_extract_page_text_invalid_file` 测试方法

### tests/test_style_extraction.py

1. **修改第27行**：将 `pdf_page = pdf_extractor.extract_page_text(1)` 替换为 `pdf_page = pdf_extractor.extract_text(pages=[1]).pages[0]`

## 预期结果

* `extract_page_text` 方法被完全移除
* 所有代码不再使用 `extract_page_text` 方法
* 所有调用都使用 `extract_text` 方法代替
* 所有测试仍然通过

