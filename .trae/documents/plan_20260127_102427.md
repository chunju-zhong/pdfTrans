# 修改Word生成器，移除页码标题但保留分页符

## 1. 实现步骤

### 1.1 修改Word生成器

- 修改 `modules/docx_generator.py` 文件
- 移除页码标题的添加逻辑（删除 `doc.add_heading(f'第 {page_num} 页', level=2)` 行）
- 保留分页符的添加逻辑
- 优化内容处理逻辑，直接按页码顺序添加文本块和图像

### 1.2 测试修改

- 运行现有的测试用例，确保修改后的Word生成器仍然正常工作
- 测试生成的Word文档，确保不包含页码标题但保留分页符
- 验证文档内容与合并后的翻译结果一致

## 2. 核心修改点

### 2.1 移除页码标题

删除 `generate_docx` 方法中添加页码标题的代码行，避免在Word文档中添加原PDF中不存在的内容。

### 2.2 保留分页符

保留 `generate_docx` 方法中添加分页符的代码行，确保每页内容之间有适当的分隔。

### 2.3 优化内容处理

保持按页码分组处理内容的逻辑，但移除页码标题的添加。

## 3. 预期结果

- 生成的Word文档不包含页码标题
- 保留每页之间的分页符
- 文档内容与合并后的翻译结果一致
- 保留原PDF的文字样式和大小
- 包含原PDF中的图像
- 保持良好的文档格式和布局