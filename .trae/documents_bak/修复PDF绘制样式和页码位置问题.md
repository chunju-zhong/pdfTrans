## 问题分析
1. **字体样式问题**：`_draw_translated_text_v2` 方法总是使用第一个文本块的样式（第206行 `base_style = translated_content['text_blocks'][0]`），导致所有块都使用相同样式，无法保留原文档的粗体、大小等样式差异。
2. **页码位置问题**：页码块未被正确处理，可能在块关联过程中位置信息丢失或错误匹配。

## 修复方案
### 1. 修复字体样式处理逻辑
- 修改 `_draw_translated_text_v2` 方法，确保每个块获取到对应位置的样式信息
- 改进块级关联算法，根据块位置（bbox）进行更精确的匹配

### 2. 修复页码位置问题
- 确保页码块在翻译和绘制过程中保持原始位置信息
- 优化块关联逻辑，确保页码块正确匹配

## 实现步骤
1. **修改 `_draw_translated_text_v2` 方法**：
   - 替换第206行的错误样式获取逻辑
   - 实现基于位置的块样式匹配算法
   - 确保每个块使用自己对应的样式信息

2. **优化块级关联逻辑**：
   - 遍历 `translated_content['text_blocks']`，为每个块找到最匹配的样式信息
   - 考虑块的位置、大小等因素进行匹配
   - 确保页码块得到正确处理

3. **测试验证**：
   - 运行测试套件验证修复效果
   - 手动测试确保字体样式和页码位置正确

## 预期效果
- 新PDF将按原文档的字体粗体、大小等样式绘制
- 页码位置将保持原文档的右下角位置
- 所有文本块将正确匹配各自的样式信息