1. **修改日志打印时机**：将`extract_text`、`extract_page_text`和`extract_text_blocks`方法中的日志打印移到调用`_mark_non_body_text`方法之后，确保日志显示的是标记后的结果

2. **优化日期格式识别**：修改`_identify_header_footer`方法中的日期正则表达式，使其能匹配"November 2025"这种只有月份和年份的格式

3. **增强页码识别逻辑**：优化`_identify_page_numbers`方法，使其能识别包含在其他文本中的页码，并考虑字体大小和位置因素

4. **添加详细日志**：在`_mark_non_body_text`方法中添加`INFO`级别的日志，显示标记后的非正文文本块数量和具体内容

5. **验证修复效果**：运行测试脚本验证修复后的识别效果，确保页眉页脚和页码被正确识别为非正文文本块