## 优化PDF提取功能以支持指定页面提取

### 实现方案

1. **更新`extract_text`方法的核心逻辑**
   - 在PyMuPDF循环中添加页面过滤
   - 在pdfplumber表格提取中添加页面过滤
   - 实现页码范围验证
   - 处理重复页码去重

2. **实现步骤**
   - 在`extract_text`方法中，添加页码验证和去重逻辑
   - 修改PyMuPDF循环，只处理指定页面
   - 修改pdfplumber表格提取，只处理指定页面
   - 保持非正文文本标记逻辑不变

3. **代码修改点**
   - 在PyMuPDF处理前验证页码范围
   - 只迭代处理指定页面
   - 表格提取时只处理指定页面
   - 保持向后兼容性

4. **测试计划**
   - 测试提取单个页面
   - 测试提取多个页面
   - 测试提取所有页面（默认行为）
   - 测试无效页码处理

### 预期效果

- 支持前端UI指定页码提取
- 提高提取效率
- 保持向后兼容性
- 提供清晰的错误处理