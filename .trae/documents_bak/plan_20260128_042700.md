## 实现步骤

### 1. 在 config.py 中添加文档类型配置
- 添加 `DEFAULT_DOC_TYPE` 配置项，默认为 "AI技术"
- 添加 `SUPPORTED_DOC_TYPES` 配置项，定义支持的文档类型列表
- 从环境变量中读取配置，允许用户覆盖默认值

### 2. 更新翻译器基类和实现类
- 修改 `translator.py` 中的 `translate` 方法，移除硬编码默认值
- 修改所有翻译器实现类（baidu/aiping/silicon_flow），移除硬编码默认值
- 确保翻译器类使用传入的 doc_type 参数

### 3. 更新翻译服务
- 修改 `translation_service.py` 中的 `process_translation` 方法，使用配置的默认值
- 确保从 app.py 传递的 doc_type 参数正确传递给翻译器

### 4. 更新应用入口
- 修改 `app.py` 中的默认值获取，使用配置的默认值
- 确保前端传递的 doc_type 参数正确处理

### 5. 更新前端模板
- 确保 `index.html` 中的默认值与配置一致

### 6. 更新测试
- 修改测试文件中的硬编码值，使用配置的默认值

## 预期结果
- 所有文档类型默认值集中在 config.py 中管理
- 支持通过环境变量覆盖默认文档类型
- 代码中不再有硬编码的文档类型值
- 所有组件使用一致的默认文档类型
- 配置更加灵活和可维护